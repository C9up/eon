[package]
name = "eon-query"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "TDengine SQL query compiler for the Ream framework (eon time-series)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
